<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="Cache-Control" content="max-age=31536000">
    <base href="/" />

    <style>
        html, body {
            margin: 0;
            padding: 0;
            height: 100%;
            background: #667eea;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            font-family: system-ui, -apple-system, sans-serif;
            overflow: hidden;
        }

        .gradient-bg {
            min-height: 100vh;
            margin: 0;
            padding: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .glass-card {
            background: rgba(255, 255, 255, 0.98);
            border-radius: 16px;
            width: 100%;
            max-width: 380px;
            padding: 1.5rem;
            box-shadow: 0 8px 24px rgba(0,0,0,0.12);
            min-height: 450px;
            height: 450px;
        }

        @@keyframes pulse {
            0%

        {
            opacity: 1;
        }

        50% {
            opacity: 0.5;
        }

        100% {
            opacity: 1;
        }

        }

       
        .password-toggle-btn::before {
            content: '';
            display: inline-block;
            width: 20px;
            height: 20px;
        }
    </style>

 
    <link rel="preload" href="bootstrap/bootstrap.min.css" as="style" onload="this.onload=null;this.rel='stylesheet'">

   
    <link rel="stylesheet" href="app.css" media="all">
    <link rel="stylesheet" href="login.css" media="all">
    <link rel="stylesheet" href="Testdotnet.styles.css" media="all">

  
    <link rel="preload" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" as="style" onload="this.onload=null;this.rel='stylesheet'">

    <link rel="preconnect" href="https://cdn.jsdelivr.net">
    <link rel="dns-prefetch" href="https://cdn.jsdelivr.net">
    <link rel="icon" type="image/png" href="favicon.png" />
    <HeadOutlet />
</head>
<body>
   
    <div id="app-loading">
        <div class="gradient-bg">
            <div class="glass-card">
                <div style="animation: pulse 1.5s ease-in-out infinite;">
                    <div style="background: #e0e0e0; height: 24px; width: 60%; border-radius: 4px; margin-bottom: 1rem;"></div>
                    <div style="background: #e0e0e0; height: 16px; width: 80%; border-radius: 4px; margin-bottom: 1rem;"></div>
                    <div style="background: #e0e0e0; height: 48px; border-radius: 4px; margin-bottom: 1rem;"></div>
                    <div style="background: #e0e0e0; height: 48px; border-radius: 4px; margin-bottom: 1rem;"></div>
                    <div style="background: #e0e0e0; height: 52px; border-radius: 4px;"></div>
                </div>
            </div>
        </div>
    </div>

    <Routes />

    <script>
        window.addEventListener('load', function() {
            setTimeout(function() {
                var loadingElement = document.getElementById('app-loading');
                if (loadingElement) loadingElement.style.display = 'none';
            }, 100);
        });
        setTimeout(function() {
            var loadingElement = document.getElementById('app-loading');
            if (loadingElement && loadingElement.style.display !== 'none') {
                loadingElement.style.display = 'none';
            }
        }, 3000);
    </script>

    <script src="_framework/blazor.web.js" defer></script>
</body>
</html>